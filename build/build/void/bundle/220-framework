# web framework packages
# sdothum - 2016 (c) wtfpl

# see bundle_install


= database management
= ═══════════════════
  # pgadmin3        [server || test -d /srv/http/tt-rss ]
  postgresql        [server || test -d /srv/http/tt-rss ]
  postgresql-client [server || test -d /srv/http/tt-rss ]
  sqlite
  sqlitebrowser


= sinatra web framework
= ═════════════════════
  g kramdown
  g mail
  g rack
  d g sanitize
  d g sinatra
  g slim
  g term-ansicolor
  g thin
  g xapian-fu
  d g xapian-ruby


= web server
= ══════════
  d g passenger
  php
  php-fpm
  # php-gd   [server || test -d /srv/http/tt-rss ]
  # php-intl [server || test -d /srv/http/tt-rss ]
  php-pgsql  [server || test -d /srv/http/tt-rss ]


# vim: set ft=conf: #
