#!/usr/local/bin/fish

log "... script installing gems"

log "... installing testing and profiling gems"
#install graphviz
#sudo gem install chronic
#sudo gem install churn
#sudo gem install flay
#sudo gem install flog
#sudo gem install gettext
#sudo gem install simplecov
#sudo gem install reek
#sudo gem install ripper_ruby_parser
#sudo gem install roodi
#sudo gem install ruby2ruby
#sudo gem install ruby_parser
#sudo gem install saikuro
#sudo gem install sexp_processor
#sudo gem install shoes
sudo gem install simple-tracer
sudo gem install term-ansicolor

log "... installing web frameworks gems"
install libxapian-ruby1.9.1
install libxslt1-dev
#sudo gem install albino
#sudo gem install builder
#sudo gem install curl
#sudo gem install fastthread
sudo gem install kramdown
#sudo gem install nokogiri
#sudo gem install padrino
#sudo gem install rack
#sudo gem install rack-perftools_profiler
#sudo gem install redcarpet
sudo gem install sanitize
#sudo gem install shotgun
sudo gem install sinatra
sudo gem install slim
sudo gem install thin
#sudo gem install tilt
sudo gem install xapian-fu

log "... installing phusion passenger"
install libcurl4-openssl-dev libssl-dev
sudo gem install passenger --version 3.0.19
sudo passenger-install-nginx-module
original /opt/nginx/conf/nginx.conf
sudo cp -v /home/shum/.config/opt/nginx/conf/nginx.conf /opt/nginx/conf/
sudo cp -v /srv/www/init-deb.sh /etc/init.d/nginx
sudo update-rc.d nginx defaults

