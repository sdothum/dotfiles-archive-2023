#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl

# Shell
# ══════════════════════════════════════════════════════════════════════════════

# ........................................................................... cd

# Usage: . W <executable>

NOTFOUND=/tmp/W:notfound

rm -f $NOTFOUND
cd $(dirname $(which $1 2>/dev/null || touch $NOTFOUND) 2>/dev/null)
[ -e $NOTFOUND ] && ditto "$1" 'not found'

# vim: set ft=sh: #
