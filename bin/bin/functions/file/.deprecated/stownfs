#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl

# File
# ▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂

# ............................................................... Sync nfs share

# Usage: stownfs [<path>]

stow=$HOME/.stownfs
[ -e $stow ] || touch $stow

# tightly integrated with vim and the dev v script
if [ $1 ] ;then
  echo $1 | grep -q "^$HOME/stow" || exit
  grep $1 $stow || echo $1 >>$stow
else
  for i in $(sort -u $stow)
  do
    rsync $i $HOME/stownfs${i#$HOME/stow}
  done
  rm $stow
fi


# vim: set ft=sh: #
