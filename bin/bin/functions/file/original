#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl

# File
# ▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂▂

# ......................................................... Distribution install

usage() {
  echo "usage: $(basename $0) [-f] <file>"
  exit 1
}

# replace previous original?
if [ $1. = -f. ] ;then
  shift
  sudo rm $1.original
fi
[ "$1" ] || usage &&
            [ -e $1.original ] && exit 1

# save distribution original
if [ -e $1 ] ;then
  ditto "copying $(sudo cp -pRv $1 $1.original)"
  exit 0
fi
exit 1

# vim: set ft=sh: #
