#!/usr/bin/dash
# sdothum - 2016 (c) wtfpl

# UI
# ══════════════════════════════════════════════════════════════════════════════

# .................................................................... Clipboard

# gpaste history add not available on raspberry pi, and xsel hangs(?) script
# cpu arm && echo -n "$@" | xsel -b || gpaste-client add "$@"
cpu arm || gpaste-client add "$@"

# vim: set ft=sh: #
