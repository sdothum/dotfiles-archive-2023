# sdothum - 2016 (c) wtfpl
[ $TRACE ] && CHILD="toggle/focus $@" . $PARENT

# herbstluftwm
# ══════════════════════════════════════════════════════════════════════════════

# ......................................................... Toggle dynamic focus

# Usage: toggle focus [fsplit | status]

status() { 
  if [ -e $DYNAMIC ] ;then
    [ -s $DYNAMIC ] && notify "Dynamic Frames" On || notify "Dynamic Focus" On
  else
    notify low "Dynamic Focus" Off
  fi
}

toggle() { 
  if [ $1 ] ;then
    [ -s $DYNAMIC ] && { rm $DYNAMIC; touch $DYNAMIC; } || echo frames >$DYNAMIC  # see focus frame
  else
    [ -e $DYNAMIC ] && rm $DYNAMIC || touch $DYNAMIC
  fi
  status
}

case $1 in
''    ) toggle; [ -e $DYNAMIC ] && focus window ;;  # see emit_hook and focus functions
fsplit) toggle frames ;;
status) status ;;
esac

# vim: set ft=sh: #
