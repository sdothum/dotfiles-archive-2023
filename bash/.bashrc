#
# ~/.bashrc
#

# If not running interactively, don't do anything
[[ $- != *i* ]] && return

alias ls='ls --color=auto'
PS1='[\u@\h \W]\$ '

# Inherit path from bash environment and /etc/profile.d
which fish >/dev/null && fish && exit
